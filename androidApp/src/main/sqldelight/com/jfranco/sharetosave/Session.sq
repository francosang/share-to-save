CREATE TABLE session (
  id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  created TEXT NOT NULL
);


selectAll:
SELECT *
FROM session ORDER BY id DESC;

create:
INSERT INTO session(created)
VALUES (?);
